name: aplikasi_anak7g
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  cupertino_icons: ^1.0.8
  
  # 📊 Tambahan Chart
  fl_chart: ^0.68.0

  # 📸 Tambahan untuk ambil foto dari galeri/kamera
  image_picker: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.3.3
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
